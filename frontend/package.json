{
  "scripts": {
    "build": "react-scripts build",
    "build-django": "react-scripts build && python manage.py collectstatic --noinput",
    "start": "react-scripts start",
    "test": "react-scripts test"
  }
}